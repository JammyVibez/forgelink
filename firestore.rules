rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for sandbox development to prevent "insufficient permissions" errors
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
